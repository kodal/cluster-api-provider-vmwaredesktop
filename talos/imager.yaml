arch: arm64
platform: nocloud
secureboot: false
version: ${TALOS_VERSION}
customization:
  extraKernelArgs:
    - net.ifnames=0
input:
  baseInstaller:
    imageRef: ghcr.io/siderolabs/installer:${TALOS_VERSION}
  systemExtensions:
    - imageRef: ghcr.io/siderolabs/talos-vmtoolsd:v1.2.0
output:
  kind: iso
  outFormat: raw
