arch: arm64
platform: vmware
secureboot: false
version: ${TALOS_VERSION}
customization:
  extraKernelArgs:
    - talos.config=guestinfo
input:
  baseInstaller:
    imageRef: ghcr.io/siderolabs/installer:${TALOS_VERSION}
  systemExtensions:
    - imageRef: ghcr.io/kodal/vmtoolsd-guest-agent:v1.3.0
output:
  kind: installer
  outFormat: raw
